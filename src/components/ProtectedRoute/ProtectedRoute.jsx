// import {Route, Redirect} from 'react-router-dom';

// const ProtectedRoute = ({children, ...rest}) => {
//   const isAuthorized = false;
//   return(
//     <Route {...rest} render={({location})=> isAuthorized ? children : <Redirect to={{pathname:"/login", state: {from: location}}}/>)}/>}
//   )
// }
